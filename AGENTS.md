## Secrets and environment files

- Never read, open, print, search, modify, or inspect `.env` or `.env.*` files containing secrets.
- Use `.env.example` to understand required environment variables.
- Never output API keys, tokens, credentials, or other secrets.
- If a task requires a secret value, ask me to perform that step manually.
